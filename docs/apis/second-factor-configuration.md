<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Second Factor Configuration

6 operation(s). Part of the [API index](../API.md).

Call these as `$client-><service>-><method>(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `getDuoAuthenticatorConfiguration`

**GET** `/v2/manage/2fa/duo-authenticator-settings`

Retrieve Duo configuration

Retrieves the Duo Authentication configuration for a specific Tenant.

### Example

```php
function callGetDuoAuthenticatorConfiguration($client) {
    try {
        $response = $client->secondFactorConfiguration->getDuoAuthenticatorConfiguration();
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Returns

`DuoSecurityAuthenticator`

---

## `getSecondFactorConfiguration`

**GET** `/v2/manage/2fa/config`

Retrieve second factor configuration

Retrieves the second factor authentication configuration for the Tenant.

### Example

```php
function callGetSecondFactorConfiguration($client) {
    try {
        $response = $client->secondFactorConfiguration->getSecondFactorConfiguration();
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Returns

`MFASettings`

---

## `getTOTPConfiguration`

**GET** `/v2/manage/2fa/totp-authenticator-settings`

Retrieve TOTP configuration

Retrieves the Time-based One Time Password (TOTP) configuration for a specific Tenant.

### Example

```php
function callGetTOTPConfiguration($client) {
    try {
        $response = $client->secondFactorConfiguration->getTOTPConfiguration();
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Returns

`GoogleAuthenticator`

---

## `updateDuoAuthenticatorConfiguration`

**PUT** `/v2/manage/2fa/duo-authenticator-settings`

Update Duo configuration

Updates the Duo Authentication configuration for a specific Tenant.

### Example

```php
function callUpdateDuoAuthenticatorConfiguration($client) {
    $duoSecurityAuthenticator = new \LoginRadius\Sdk\Internal\OpenApi\Model\DuoSecurityAuthenticator(['isEnabled' => true, 'clientId' => '<clientId>', 'clientSecret' => '<clientSecret>']); //Required

    try {
        $response = $client->secondFactorConfiguration->updateDuoAuthenticatorConfiguration($duoSecurityAuthenticator);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Request body

`DuoSecurityAuthenticator` as `application/json`.

### Returns

`DuoSecurityAuthenticator`

---

## `updateSecondFactorConfiguration`

**PUT** `/v2/manage/2fa/config`

Update second factor configuration

Updates the second factor authentication configuration for the Tenant.

### Example

```php
function callUpdateSecondFactorConfiguration($client) {
    $mFASettings = new \LoginRadius\Sdk\Internal\OpenApi\Model\MFASettings(['isSecondFactorAuthenticatorEnabled' => true, 'isRequired' => true, 'isAuthenticatorEnabled' => true]); //Required

    try {
        $response = $client->secondFactorConfiguration->updateSecondFactorConfiguration($mFASettings);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Request body

`MFASettings` as `application/json`.

### Returns

`MFASettings`

---

## `updateTOTPConfiguration`

**PUT** `/v2/manage/2fa/totp-authenticator-settings`

Update TOTP configuration

Updates the Time-based One Time Password (TOTP) configuration for a specific Tenant.

### Example

```php
function callUpdateTOTPConfiguration($client) {
    $googleAuthenticator = new \LoginRadius\Sdk\Internal\OpenApi\Model\GoogleAuthenticator(['issuerId' => '<issuerId>']); //Required

    try {
        $response = $client->secondFactorConfiguration->updateTOTPConfiguration($googleAuthenticator);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Request body

`GoogleAuthenticator` as `application/json`.

### Returns

`GoogleAuthenticator`

