<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Organization

8 operation(s). Part of the [API index](../API.md).

Call these as `$client-><service>-><method>(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `createOrganization`

**POST** `/v2/manage/organizations`

Create Organization

Creates a new Organization in the Tenant.

### Example

```php
function callCreateOrganization($client) {
    $createOrganizationRequest = new \LoginRadius\Sdk\Internal\OpenApi\Model\CreateOrganizationRequest(['name' => '<name>']); //Required

    try {
        $response = $client->organization->createOrganization($createOrganizationRequest);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Request body

`object` as `application/json`.

### Returns

`OrganizationsResponse`

---

## `createOrgTenantRole`

**POST** `/v2/manage/organizations/{orgId}/roles`

Create Role in Organization

Creates a Role within an Organization.

### Example

```php
function callCreateOrgTenantRole($client) {
    $orgId = '<orgId>'; //Required
    $rolePostRequest = new \LoginRadius\Sdk\Internal\OpenApi\Model\RolePostRequest(['name' => '<name>']); //Required

    try {
        $response = $client->organization->createOrgTenantRole($rolePostRequest, orgId: $orgId);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Request body

`RolePostRequest` as `application/json`.

### Returns

`Role`

---

## `deleteOrganization`

**DELETE** `/v2/manage/organizations/{orgId}`

Delete Organization

Deletes an Organization by its ID.

### Example

```php
function callDeleteOrganization($client) {
    $orgId = '<orgId>'; //Required

    try {
        $response = $client->organization->deleteOrganization(orgId: $orgId);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Returns

`DeleteResponse`

---

## `getAllOrganizations`

**GET** `/v2/manage/organizations`

List Organizations

Retrieves a list of all Organizations in the Tenant.

### Example

```php
function callGetAllOrganizations($client) {
    try {
        $response = $client->organization->getAllOrganizations();
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Returns

`object`

---

## `getOrganization`

**GET** `/v2/manage/organizations/{orgId}`

Retrieve Organization details

Retrieves details of a specific Organization by its ID.

### Example

```php
function callGetOrganization($client) {
    $orgId = '<orgId>'; //Required

    try {
        $response = $client->organization->getOrganization(orgId: $orgId);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Returns

`OrganizationsResponse`

---

## `getOrgContextByOrgId`

**GET** `/v2/manage/organizations/{orgId}/orgcontext`

Retrieve Organization context

Retrieves User Roles for all Organizations by OrgID.

### Example

```php
function callGetOrgContextByOrgId($client) {
    $orgId = '<orgId>'; //Required

    try {
        $response = $client->organization->getOrgContextByOrgId(orgId: $orgId);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Unique identifier of the Organization. |

### Returns

`object`

---

## `getOrgRolesByOrgId`

**GET** `/v2/manage/organizations/{orgId}/roles`

List Organization Roles

Lists all Roles defined within an Organization.

### Example

```php
function callGetOrgRolesByOrgId($client) {
    $orgId = '<orgId>'; //Required

    try {
        $response = $client->organization->getOrgRolesByOrgId(orgId: $orgId);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Returns

`object`

---

## `updateOrganization`

**PUT** `/v2/manage/organizations/{orgId}`

Update Organization

Updates an Organization by its ID. Supports updating org fields, policies, and status in a single request.

### Example

```php
function callUpdateOrganization($client) {
    $orgId = '<orgId>'; //Required
    $organizationUpdateRequest = new \LoginRadius\Sdk\Internal\OpenApi\Model\OrganizationUpdateRequest(['name' => '<name>', 'display' => '<display>', 'metadata' => '<metadata>']); //Required

    try {
        $response = $client->organization->updateOrganization($organizationUpdateRequest, orgId: $orgId);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Request body

`OrganizationUpdateRequest` as `application/json`.

### Returns

`OrganizationsResponse`

