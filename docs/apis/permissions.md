<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Permissions

5 operation(s). Part of the [API index](../API.md).

Call these as `$client-><service>-><method>(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `addPermission`

**POST** `/v2/manage/permissions`

Create Permission

Adds a new Permission.

### Example

```php
function callAddPermission($client) {
    $permissionsPostRequest = new \LoginRadius\Sdk\Internal\OpenApi\Model\PermissionsPostRequest(['name' => '<name>']); //Optional

    try {
        $response = $client->permissions->addPermission($permissionsPostRequest);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Request body

`PermissionsPostRequest` as `application/json`.

### Returns

`Permissions`

---

## `deleteTenantPermission`

**DELETE** `/v2/manage/permissions/{id}`

Delete Permission

Deletes a specific Permission.

### Example

```php
function callDeleteTenantPermission($client) {
    $id = '<id>'; //Required

    try {
        $response = $client->permissions->deleteTenantPermission(id: $id);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `id` | path | string | yes | The unique identifier for the Permission |

### Returns

`DeleteResponse`

---

## `getPermissionById`

**GET** `/v2/manage/permissions/{id}`

Retrieve Permission by ID

Retrieves a Permission by its ID.

### Example

```php
function callGetPermissionById($client) {
    $id = '<id>'; //Required

    try {
        $response = $client->permissions->getPermissionById(id: $id);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `id` | path | string | yes | The unique identifier for the Permission |

### Returns

`Permissions`

---

## `permissions`

**GET** `/v2/manage/permissions`

List Permissions

Retrieves a list of all Permissions.

### Example

```php
function callPermissions($client) {
    try {
        $response = $client->permissions->permissions();
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Returns

`object`

---

## `updateTenantPermission`

**PUT** `/v2/manage/permissions/{id}`

Update Permission

Updates a specific Permission. Note: The Name field cannot be modified for non-B2B apps. If a different Name value is provided, the API will return an error.

### Example

```php
function callUpdateTenantPermission($client) {
    $id = '<id>'; //Required
    $permissionPutRequest = new \LoginRadius\Sdk\Internal\OpenApi\Model\PermissionPutRequest(['name' => '<name>', 'description' => '<description>']); //Optional

    try {
        $response = $client->permissions->updateTenantPermission($permissionPutRequest, id: $id);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `id` | path | string | yes | The unique identifier for the Permission |

### Request body

`PermissionPutRequest` as `application/json`.

### Returns

`Permissions`

