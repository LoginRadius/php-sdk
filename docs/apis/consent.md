<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Consent

7 operation(s). Part of the [API index](../API.md).

Call these as `$client-><service>-><method>(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `addConsentForm`

**POST** `/consent/forms`

Add Consent Form

Adds a new Consent Form for the Tenant.

### Example

```php
function callAddConsentForm($client) {
    $consentFormModel = new \LoginRadius\Sdk\Internal\OpenApi\Model\ConsentFormModel(['events' => '<events>', 'startFromDate' => '<startFromDate>', 'consentOptions' => '<consentOptions>']); //Required

    try {
        $response = $client->consent->addConsentForm($consentFormModel);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Request body

`ConsentFormModel` as `application/json`.

### Returns

`ConsentForm`

---

## `createConsentOption`

**POST** `/consent/options`

Create Consent Option

Creates a new consent option for a specific Tenant.

### Example

```php
function callCreateConsentOption($client) {
    $consentOptionModel = new \LoginRadius\Sdk\Internal\OpenApi\Model\ConsentOptionModel(['title' => '<title>', 'description' => '<description>']); //Required

    try {
        $response = $client->consent->createConsentOption($consentOptionModel);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Request body

`ConsentOptionModel` as `application/json`.

### Returns

`ConsentOptions`

---

## `deleteConsentForm`

**DELETE** `/consent/forms/{version}`

Delete Consent Form

Deletes the Consent Form identified by the form version for the Tenant.

### Example

```php
function callDeleteConsentForm($client) {
    $version = '<version>'; //Required

    try {
        $response = $client->consent->deleteConsentForm(version: $version);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `version` | path | string | yes | The version of the Consent form to delete. |

### Returns

`IsDeleted`

---

## `deleteConsentOption`

**DELETE** `/consent/options/{optionId}`

Delete Consent Option

Deletes the consent option identified by the option ID for the Tenant.

### Example

```php
function callDeleteConsentOption($client) {
    $optionId = '<optionId>'; //Required

    try {
        $response = $client->consent->deleteConsentOption(optionId: $optionId);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `optionId` | path | string | yes | The ID of the Consent option to delete. |

### Returns

`IsDeleted`

---

## `getActiveConsentForms`

**GET** `/consent/forms/active`

Retrieve Active Consent Forms

Retrieves a list of active Consent Forms configured for the Tenant.

### Example

```php
function callGetActiveConsentForms($client) {
    $event = '<event>'; //Required

    try {
        $response = $client->consent->getActiveConsentForms(event: $event);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `event` | query | string | yes | Event type to filter consent verification (e.g., `login`). |

### Returns

`object`

---

## `getConsentForms`

**GET** `/consent/forms`

Retrieve Consent Forms

Retrieves all Consent Forms configured for the Tenant.

### Example

```php
function callGetConsentForms($client) {
    try {
        $response = $client->consent->getConsentForms();
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Returns

`object`

---

## `getConsentOptions`

**GET** `/consent/options`

Retrieve Consent Options

Lists all consent options available for a specific Tenant.

### Example

```php
function callGetConsentOptions($client) {
    try {
        $response = $client->consent->getConsentOptions();
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Returns

`object`

