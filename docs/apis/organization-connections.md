<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Organization Connections

6 operation(s). Part of the [API index](../API.md).

Call these as `$client-><service>-><method>(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `createOrganizationConnection`

**POST** `/v2/manage/organizations/{orgId}/connections`

Create Organization connection

Creates a new Identity Provider connection for an Organization.

### Example

```php
function callCreateOrganizationConnection($client) {
    $orgId = '<orgId>'; //Required
    $organizationConnectionCreateRequest = new \LoginRadius\Sdk\Internal\OpenApi\Model\OrganizationConnectionCreateRequest(['name' => '<name>', 'domain' => '<domain>', 'connectionType' => '<connectionType>']); //Required

    try {
        $response = $client->organizationConnections->createOrganizationConnection($organizationConnectionCreateRequest, orgId: $orgId);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Request body

`OrganizationConnectionCreateRequest` as `application/json`.

### Returns

`ConnectionResponse`

---

## `deleteOrganizationConnection`

**DELETE** `/v2/manage/organizations/{orgId}/connections/{connId}`

Delete Organization connection

Deletes an Identity Provider connection from an Organization.

### Example

```php
function callDeleteOrganizationConnection($client) {
    $orgId = '<orgId>'; //Required
    $connId = '<connId>'; //Required

    try {
        $response = $client->organizationConnections->deleteOrganizationConnection(orgId: $orgId, connId: $connId);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |
| `connId` | path | string | yes | Organization Connection ID |

### Returns

`DeleteResponse`

---

## `getAllOrganizationConnections`

**GET** `/v2/manage/organizations/{orgId}/connections`

List Organization connections

Lists all Identity Provider connections for an Organization.

### Example

```php
function callGetAllOrganizationConnections($client) {
    $orgId = '<orgId>'; //Required

    try {
        $response = $client->organizationConnections->getAllOrganizationConnections(orgId: $orgId);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Returns

`object`

---

## `getOrganizationConnection`

**GET** `/v2/manage/organizations/{orgId}/connections/{connId}`

Retrieve Organization connection

Retrieves details of a specific Identity Provider connection.

### Example

```php
function callGetOrganizationConnection($client) {
    $orgId = '<orgId>'; //Required
    $connId = '<connId>'; //Required

    try {
        $response = $client->organizationConnections->getOrganizationConnection(orgId: $orgId, connId: $connId);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |
| `connId` | path | string | yes | Organization Connection ID |

### Returns

`ConnectionResponse`

---

## `updateConnectionStatus`

**PUT** `/v2/manage/organizations/{orgId}/connections/{connId}/status`

Update Organization connection status

Updates the active status of an Identity Provider connection.

### Example

```php
function callUpdateConnectionStatus($client) {
    $orgId = '<orgId>'; //Required
    $connId = '<connId>'; //Required
    $connectionStatusRequest = new \LoginRadius\Sdk\Internal\OpenApi\Model\ConnectionStatusRequest(['active' => true]); //Required

    try {
        $response = $client->organizationConnections->updateConnectionStatus($connectionStatusRequest, orgId: $orgId, connId: $connId);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |
| `connId` | path | string | yes | Organization Connection ID |

### Request body

`ConnectionStatusRequest` as `application/json`.

### Returns

`ConnectionStatusResponse`

---

## `updateOrganizationConnection`

**PUT** `/v2/manage/organizations/{orgId}/connections/{connId}`

Update Organization connection

Updates the configuration of an Identity Provider connection.

### Example

```php
function callUpdateOrganizationConnection($client) {
    $orgId = '<orgId>'; //Required
    $connId = '<connId>'; //Required
    $organizationConnectionRequest = new \LoginRadius\Sdk\Internal\OpenApi\Model\OrganizationConnectionRequest(['name' => '<name>', 'domain' => '<domain>', 'attributes' => '<attributes>']); //Required

    try {
        $response = $client->organizationConnections->updateOrganizationConnection($organizationConnectionRequest, orgId: $orgId, connId: $connId);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |
| `connId` | path | string | yes | Organization Connection ID |

### Request body

`OrganizationConnectionRequest` as `application/json`.

### Returns

`ConnectionResponse`

