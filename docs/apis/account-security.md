<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Account Security

10 operation(s). Part of the [API index](../API.md).

Call these as `$client-><service>-><method>(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `mfaGenerateBackupCodesByUid`

**GET** `/identity/v2/manage/account/2fa/backupcode`

Generate Backup Codes

Generates a set of backup codes for the specified User.

### Example

```php
function callMfaGenerateBackupCodesByUid($client) {
    $uid = '<uid>'; //Required

    try {
        $response = $client->accountSecurity->mfaGenerateBackupCodesByUid(uid: $uid);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | query | string | yes | The UID associated with the User |

### Returns

`MFABackUpCodeResponse`

---

## `mfaResetBackupCodesByUid`

**GET** `/identity/v2/manage/account/2fa/backupcode/reset`

Reset Backup Codes

Resets and generates a new set of backup codes for the specified User.

### Example

```php
function callMfaResetBackupCodesByUid($client) {
    $uid = '<uid>'; //Required

    try {
        $response = $client->accountSecurity->mfaResetBackupCodesByUid(uid: $uid);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | query | string | yes | The UID associated with the User |

### Returns

`MFABackUpCodeResponse`

---

## `mFAResetSMSAuthByUid`

**DELETE** `/identity/v2/manage/account/2fa/sms`

Reset SMS Authenticator

Resets MFA settings for the specified User.

### Example

```php
function callMFAResetSMSAuthByUid($client) {
    $uid = '<uid>'; //Required

    try {
        $response = $client->accountSecurity->mFAResetSMSAuthByUid(uid: $uid);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | query | string | yes | The UID associated with the User |

### Returns

`IsDeleted`

---

## `mFAResetTotpByUid`

**DELETE** `/identity/v2/manage/account/2fa/totp`

Reset TOTP

Resets MFA settings for the specified User.

### Example

```php
function callMFAResetTotpByUid($client) {
    $uid = '<uid>'; //Required

    try {
        $response = $client->accountSecurity->mFAResetTotpByUid(uid: $uid);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | query | string | yes | The UID associated with the User |

### Returns

`IsDeleted`

---

## `resetDuoAuthByUid`

**DELETE** `/identity/v2/manage/account/2fa/duo`

Reset Duo

Resets the Duo Authenticator for the specified User.

### Example

```php
function callResetDuoAuthByUid($client) {
    $uid = '<uid>'; //Required

    try {
        $response = $client->accountSecurity->resetDuoAuthByUid(uid: $uid);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | query | string | yes | The UID associated with the User |

### Returns

`IsDeleted`

---

## `resetEmailAuthenticatorByUid`

**DELETE** `/identity/v2/manage/account/2fa/email`

Reset Email OTP

Resets the Email OTP Authenticator for the specified User.

### Example

```php
function callResetEmailAuthenticatorByUid($client) {
    $uid = '<uid>'; //Required

    try {
        $response = $client->accountSecurity->resetEmailAuthenticatorByUid(uid: $uid);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | query | string | yes | The UID associated with the User |

### Returns

`IsDeleted`

---

## `resetMfaPasskeyByUid`

**DELETE** `/identity/v2/manage/account/2fa/passkey`

Reset MFA Passkey

Resets the MFA Passkey for the specified User.

### Example

```php
function callResetMfaPasskeyByUid($client) {
    $uid = '<uid>'; //Required

    try {
        $response = $client->accountSecurity->resetMfaPasskeyByUid(uid: $uid);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | query | string | yes | The UID associated with the User |

### Returns

`IsDeleted`

---

## `resetMfaPushByUid`

**DELETE** `/identity/v2/manage/account/2fa/push`

Reset MFA Push Notification

Resets the Push Notification Authenticator for the specified User.

### Example

```php
function callResetMfaPushByUid($client) {
    $uid = '<uid>'; //Required

    try {
        $response = $client->accountSecurity->resetMfaPushByUid(uid: $uid);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | query | string | yes | The UID associated with the User |

### Returns

`IsDeleted`

---

## `validateSecondFactorTokenForPassword`

**POST** `/identity/v2/manage/account/{uid}/reauth/password`

Verify Password MFA Token

Validates and verifies the 'SecondFactorValidationToken' generated by the Step-Up Authenticate By Password API.

### Example

```php
function callValidateSecondFactorTokenForPassword($client) {
    $uid = '<uid>'; //Required
    $eventBasedSecondFactorToken = new \LoginRadius\Sdk\Internal\OpenApi\Model\EventBasedSecondFactorToken(['secondfactorvalidationtoken' => '<secondfactorvalidationtoken>']); //Required

    try {
        $response = $client->accountSecurity->validateSecondFactorTokenForPassword($eventBasedSecondFactorToken, uid: $uid);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | path | string | yes | The UID associated with the User |

### Request body

`EventBasedSecondFactorToken` as `application/json`.

### Returns

`IsValid`

---

## `validateSecondFactorTokenForPin`

**POST** `/identity/v2/manage/account/{uid}/reauth/pin`

Verify PIN MFA Token

Validates and verifies the 'SecondFactorValidationToken' generated by the Step-Up Authenticate By PIN API.

### Example

```php
function callValidateSecondFactorTokenForPin($client) {
    $uid = '<uid>'; //Required
    $eventBasedSecondFactorToken = new \LoginRadius\Sdk\Internal\OpenApi\Model\EventBasedSecondFactorToken(['secondfactorvalidationtoken' => '<secondfactorvalidationtoken>']); //Required

    try {
        $response = $client->accountSecurity->validateSecondFactorTokenForPin($eventBasedSecondFactorToken, uid: $uid);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | path | string | yes | The UID associated with the User |

### Request body

`EventBasedSecondFactorToken` as `application/json`.

### Returns

`IsValid`

