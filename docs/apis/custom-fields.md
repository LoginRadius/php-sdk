<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Custom Fields

7 operation(s). Part of the [API index](../API.md).

Call these as `$client-><service>-><method>(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `createCustomField`

**POST** `/v2/manage/custom-fields`

Create custom field

Creates a new Custom Field for the Tenant.

### Example

```php
function callCreateCustomField($client) {
    $raasCustomFieldModel = new \LoginRadius\Sdk\Internal\OpenApi\Model\RaasCustomFieldModel(['customField' => '<customField>']); //Required

    try {
        $response = $client->customFields->createCustomField($raasCustomFieldModel);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Request body

`RaasCustomFieldModel` as `application/json`.

### Returns

`RaasCustomField`

---

## `deleteCustomField`

**DELETE** `/v2/manage/custom-fields/{cfname}`

Delete custom field

Deletes a Custom Field by name for the Tenant.

### Example

```php
function callDeleteCustomField($client) {
    $cfname = '<cfname>'; //Required

    try {
        $response = $client->customFields->deleteCustomField(cfname: $cfname);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `cfname` | path | string | yes | Custom Fields Name |

### Returns

`DeleteResponse`

---

## `getActiveCustomFields`

**GET** `/v2/manage/custom-fields/active`

List active custom fields

Retrieves all custom fields currently active in the registration form for the Tenant.

### Example

```php
function callGetActiveCustomFields($client) {
    try {
        $response = $client->customFields->getActiveCustomFields();
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Returns

`object`

---

## `getAllCustomFields`

**GET** `/v2/manage/custom-fields`

List custom fields

Retrieves all Custom Fields created for the Tenant.

### Example

```php
function callGetAllCustomFields($client) {
    try {
        $response = $client->customFields->getAllCustomFields();
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Returns

`object`

---

## `getCustomFieldLimit`

**GET** `/v2/manage/custom-fields/limit`

Retrieve custom field limit

Retrieves the Custom Field Limit configured for the Tenant.

### Example

```php
function callGetCustomFieldLimit($client) {
    try {
        $response = $client->customFields->getCustomFieldLimit();
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Returns

`CustomFieldLimitResponse`

---

## `listCustomFields`

**GET** `/v2/manage/custom-fields/list`

List custom fields

Retrieves all custom fields for the Tenant, returned as an array of strings.

### Example

```php
function callListCustomFields($client) {
    try {
        $response = $client->customFields->listCustomFields();
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Returns

`object`

---

## `setCustomField`

**PUT** `/v2/manage/custom-fields`

Set custom field

Updates or sets a Custom Field instance in RAAS to be displayed on forms for the Tenant.

### Example

```php
function callSetCustomField($client) {
    $setCustomFieldRequest = new \LoginRadius\Sdk\Internal\OpenApi\Model\SetCustomFieldRequest(['data' => '<data>']); //Required

    try {
        $response = $client->customFields->setCustomField($setCustomFieldRequest);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Request body

`object` as `application/json`.

### Returns

`object`

