<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Captcha Configuration

2 operation(s). Part of the [API index](../API.md).

Call these as `$client-><service>-><method>(...)`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `getCaptchaConfiguration`

**GET** `/v2/manage/captcha`

Retrieve captcha configuration

Retrieves the captcha configuration settings for a specific Tenant.

### Example

```php
function callGetCaptchaConfiguration($client) {
    try {
        $response = $client->captchaConfiguration->getCaptchaConfiguration();
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Returns

`CaptchaConfig`

---

## `updateCaptchaConfiguration`

**PUT** `/v2/manage/captcha`

Update captcha configuration

Updates the captcha configuration settings for a specific Tenant.

### Example

```php
function callUpdateCaptchaConfiguration($client) {
    $captchaConfig = new \LoginRadius\Sdk\Internal\OpenApi\Model\CaptchaConfig(['isEnabled' => true, 'qQTencentCaptcha' => '<qQTencentCaptcha>', 'googleRecaptchaV2' => '<googleRecaptchaV2>']); //Required

    try {
        $response = $client->captchaConfiguration->updateCaptchaConfiguration($captchaConfig);
        echo $response;
    } catch (\LoginRadius\Sdk\Internal\OpenApi\ApiException $e) {
        $lr = \LoginRadius\Sdk\Client::toLoginRadiusException($e);
        echo $lr->description();
    }
}
```

### Request body

`CaptchaConfig` as `application/json`.

### Returns

`CaptchaConfig`

